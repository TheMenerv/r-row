/**
 * @type DrawFunction - The function that is called every frame.
 * @param {CanvasRenderingContext2D} ctx - The canvas rendering context.
 * @returns {void}
 * @public
 */
export type DrawFunction = (ctx: CanvasRenderingContext2D) => void;
