/**
 * @type UpdateFunction - The function that is called every frame.
 * @param {number} deltaTime - The time since the last frame.
 * @returns {void}
 * @public
 */
export type UpdateFunction = (deltaTime: number) => void;
