/**
 * @interface RectangleOptions - Rectangle options
 * @property {string} [fillColor] - Fill color
 * @property {number} [weight] - Stroke weight
 * @property {string} [strokeColor] - Stroke color
 * @property {number} [radius] - Radius of the corners
 * @public
 */
export interface RectangleOptions {
  fillColor?: string;
  weight?: number;
  strokeColor?: string;
  radius?: number;
}
