/**
 * @enum SoundState - The state of a sound.
 * @property Playing - The sound is playing.
 * @property Paused - The sound is paused.
 * @property Stopped - The sound is stopped.
 * @public
 */
export enum SoundState {
  Playing = 'playing',
  Paused = 'paused',
  Stopped = 'stopped',
}
